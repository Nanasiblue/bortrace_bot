[project]
name = "bortrace-rebuild"
version = "0.1.0"
description = "Rebuild workspace for boatrace data parsing, modeling, and prediction"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
